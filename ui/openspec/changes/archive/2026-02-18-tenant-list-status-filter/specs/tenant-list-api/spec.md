## 新增需求
（无）

## 修改需求

### 需求:租户列表接口状态过滤
租户列表接口必须支持可选的 status 参数，不传参时默认只返回 active 状态的租户。

#### 场景:不传 status 参数
- **当** 调用租户列表接口不传 status 参数
- **那么** 系统只返回 active 状态的租户

#### 场景:传 status 参数
- **当** 调用租户列表接口传 status 参数
- **那么** 系统返回指定状态的租户

## 移除需求
（无）

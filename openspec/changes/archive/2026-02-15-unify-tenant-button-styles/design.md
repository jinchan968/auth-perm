## 上下文

当前返回按钮使用了 `variant="outline" size="sm"`，但右侧操作按钮（设置、编辑）也使用 `variant="outline"` 而没有指定 size（默认尺寸）。需要统一为一致的尺寸。

## 目标 / 非目标

**目标：**
- 返回按钮与操作按钮使用相同的尺寸和样式

**非目标：**
- 不改变按钮的功能

## 决策

1. **移除返回按钮的 size="sm"** - 使用默认尺寸，与其他操作按钮一致
2. **保持 outline variant** - 与其他按钮保持一致

## 风险 / 权衡

- 风险较低，仅涉及样式调整

## 新增需求

### 需求:租户禁用功能
系统必须提供租户禁用功能，允许管理员将租户状态设置为 suspended。

#### 场景:禁用租户
- **当** 管理员点击禁用按钮
- **那么** 系统将租户状态更新为 suspended

#### 场景:启用租户
- **当** 管理员点击启用按钮
- **那么** 系统将租户状态更新为 active

#### 场景:查询租户列表
- **当** 系统查询租户列表
- **那么** 默认排除状态为 suspended 的租户

## 修改需求
（无）

## 移除需求
（无）

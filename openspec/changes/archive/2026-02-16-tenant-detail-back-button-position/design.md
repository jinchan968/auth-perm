## 上下文

调整租户详情页的按钮布局，将返回按钮从左侧移到右侧删除按钮旁边。

## 目标 / 非目标

**目标：**
- 将返回按钮移到右侧操作区域（删除按钮右边）

**非目标：**
- 不改变按钮样式
- 不改变面包屑位置

## 决策

1. 将返回按钮从左侧 div 移到右侧 div
2. 右侧按钮顺序：编辑/保存/取消 → 删除 → 返回

## 风险 / 权衡

- 风险：无
- 权衡：无

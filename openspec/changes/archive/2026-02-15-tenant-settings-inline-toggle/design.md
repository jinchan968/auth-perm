## 上下文

当前租户详情页显示功能开关和配额为静态文本，点击设置按钮后需要切换到编辑态。

## 目标 / 非目标

**目标：**
- 功能开关和配额区域在查看态和编辑态之间切换
- 编辑态使用 Switch 组件和 Input 组件
- 点击设置按钮触发切换

**非目标：**
- 不修改后端 API

## 决策

1. **同一区域切换**：功能开关和配额使用同一个 Card，在查看态和编辑态之间切换
2. **设置按钮触发**：点击设置按钮切换到编辑态

## 风险 / 权衡

- 风险较低

## 为什么

租户详情页的功能开关和配额显示为静态展示态，点击设置按钮后，应该切换到编辑态，而不是显示一个独立的设置卡片。编辑态使用 Switch 组件编辑功能开关，使用 Input 编辑配额。

## 变更内容

1. **改造详情页设置区域** - 功能开关和配额区域在查看态和编辑态之间切换

## 功能 (Capabilities)

### 新增功能
- `tenant-settings-inline-toggle`: 设置区域就地编辑切换

### 修改功能
- 无

## 影响

- `ui/app/tenants/[id]/page.tsx` - 租户详情页

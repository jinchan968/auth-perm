## 新增需求

### 需求:返回按钮样式
租户详情页及子页面的返回按钮必须具有清晰、协调的视觉效果。

#### 场景:返回按钮基础样式
- **当** 用户查看租户详情页的返回按钮
- **那么** 按钮使用 outline variant，颜色与主题一致

#### 场景:返回按钮 hover 态
- **当** 用户将鼠标悬停在返回按钮上
- **那么** 按钮背景变为 slat-100，文字颜色加深

### 需求:按钮图标一致性
返回按钮必须使用统一的返回图标。

#### 场景:图标显示
- **当** 用户查看返回按钮
- **那么** 按钮显示 ArrowLeft 图标
